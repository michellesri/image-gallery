import template from './app.html';
import './app.scss';

export default {
  template
  // controller
};

// need routes.js file
  // to invoke / go to a route, can use this function below, or can use sref.



// controller.$inject = ['$state'];
//
// function controller($state) {
//   this.gotoImages = () => $state.go('images');
// }
